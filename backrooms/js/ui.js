var sys = document.getElementsByTagName("sys");
for(var i = 0;i < sys.length;i++) if(sys[i].getAttribute("class"))
{
  if(sys[i].getAttribute().search("iets") != -1)
  {
    switch(sys[i].innerHTML.search("iets"))
  }
}
